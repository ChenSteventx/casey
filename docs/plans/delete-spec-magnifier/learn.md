# learn · delete-spec-magnifier

1. **编译器形状演化要配套巡检消费侧前置闸**：删除搜索修契约给 delete 域新增放大镜
   click 时，replay 侧 delete-spec 闸没同车——形状演化的契约应列「谁消费这个形状」
   清单（本链已连破四道：基数门/准入信封/准入终端/delete-spec）。
2. **精确字面白名单胜宽松判**：放大镜以编译器 css 字面逐字比对放行，错一字即拒、
   混装即拒——白名单加法不稀释 fail-closed。
3. **首轮双 APPROVE 四连**：小契约（1 条件分支 + 六钉）从立约到双绿一小时内完成，
   工艺已成流水线。
4. **挂账**：承前（A4 白名单独立钉、票据核销态查验在 replay 重跑前）。
