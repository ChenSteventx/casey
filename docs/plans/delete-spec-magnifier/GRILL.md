# GRILL · delete-spec-magnifier（Steven 裁定定案）

裁定：十六跑 replay 前置闸 fail-closed 实证（零启动零残留）摆裁后，Steven 2026-08-08
当轮点选**闸学精确形状**（投影补携合成文案否——图标无真文案，造假语义污染事件契约）。

1. **缝的本体**：`lib/workflow-delete-spec.mjs` · `validateWorkflowDeleteBindings`
   把 `workflow.deleteByName` 域内 click 白名单钉死为三个破坏性文案（删除/确定/确认）
   且各须携目标绑定 value。删除搜索修契约（`b6880e3`）给该域新增了**放大镜 click**
   （搜索触发器：`fallbackCss: ".hr-input__suffix .search-icon"`、无文案无 value，
   过滤与删后重搜各一枚）——闸早于该形状，replay 首接 v3 真事件即拒
   `unsupported_click_label`（十五跑产物 `atstep_13`/`atstep_17` 实证）。
2. **修形（白名单加一精确形）**：文案 null ∧ `value` null/缺 ∧ `fallbackCss` 逐字等于
   编译器字面 `.hr-input__suffix .search-icon` → 放行（非破坏搜索触发器、无目标绑定
   义务）。其余 null 文案照拒；三个破坏性文案的 value 义务原样；css 对但携 value 或
   携文案 → 形状矛盾照拒（防混装）。
3. **冻结面**：`workflow-delete-spec-preflight.static` 金牌（prd-wf-delete-card-layout
   冻结）夹具不携放大镜 css、钉面与加法零交叠——预期零触碰全绿。
4. **非目标**：不动放大镜编译配方；不碰 replay.mjs 调用面；不裁 verdict/report 段。
5. **修通预期（预登记）**：重跑 replay（权威/票据可复用——十六跑 fail-closed 于
   launch 前、票据未核销；若已占用则重铸并披露）→ 相 3 真机回放首过或停于下一门
   如实报。
