# report-exit64 — learn（2026-07-07，light，codex 一轮 R1 PASS）

`report` 用法错历史码 exit 2→64 收敛，cli-mcp-face 挂账兑现。单行本体 + 金牌四钉位翻 64 + prd 重签，
红先行 0/2 → 绿 2/2，双 gate GREEN。

## 留存学习

1. 评审员自陈的盲区要主循环兜底：codex 空 cwd 无法全仓 `rg` 复验「还有没有别处钉 2」——这不是 PASS 的
   瑕疵而是分工信号，主循环补扫是评审闭环的一部分。本轮补扫真抓到一处：`SKILL.md:47` 活文档还写着
   「历史码 exit 2」，三面（CLI/MCP/skill）漂移锁只盯命令行对齐、文字描述面靠人扫——文字性漂移是
   漂移锁金牌的已知盲区，收敛类契约收尾必须全仓扫文字残留（历史档案 learn.md 不动、活文档必改）。
2. 外部评审通道会网络性卡死：codex 首轮 WebSocket 重连循环 15 分钟零输出——掐掉重发即过（26k tokens
   一轮）。判断标准：输出尾部持续 `Reconnecting`/零推理正文 = 通道死，别干等超时。
3. 挂账制的兑现闭环范本：cli-mcp-face 当时「如实标注 exit 2 + 收敛另案挂账」而非顺手改——本契约兑现时
   钉位清单（:6/:48/:56/:64）就是当时挂账留下的地图，半小时收干净。对比：若当时顺手改会把 cli-mcp-face
   的评审面撑大一圈。「如实记账 + 另案兑现」比「顺手修」在评审经济上更优。

## 交到下一阶段的账

- 无新挂账；cli-mcp-face observability 的「report 退出码收敛另案」就此核销。
- C 线下一刀：全仓输出通道系统审计（扫描子代理产出封缝清单后立契约封缝）。
