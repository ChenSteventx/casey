我将按契约与风险清单做只读异构评审，先读完整任务书与冻结设计，再逐项核查能力面与 fail-open 路径。`runtime-cycle-adapter` 已压到 295 行；接着按风险清单核 capability、fail-open 与裁判纯度。再核对 orchestrator 金牌是否漏钉 compare 入参，并确认 `inspectClaimed` 在仓内无生产实现。# teachin-replayability-closure 异构评审（Grok 主审 · 首轮）

**范围**：附录 A 白名单 + 风险清单 1–7；只读；亲跑若干零 SUT 金牌 exit 0（action-authority / capture-fresh / prepared-run / entity-verification / verdict-cli / boundaries / production-boundary / orchestrator-wiring）。  
**行数复核**：`runtime-cycle-adapter.mjs` **295**（Y5 300 已达标）、全实现面仍 <600。  
**裁判锚**：`FROZEN_JUDGE_SHA256` 与 `bin/verdict.mjs` / `prd-gen-prompts` 一致。

---

## Critical

### C1 · 生产编排 compare 调用缺 4 个闭合入参，真实 comparator 恒拒；金牌 mock 不钉死

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/dual-replay-orchestrator-core.mjs:328-333` vs `lib/dual-replay/comparator.mjs:11-14,107-112` |
| **问题** | 生产 core 只传 `pairAuthority` + 两枚 comparison grant；`compareSemanticReplayReceipts` 要求 exact 7 键（含 `sourceReceiptBytes/Authority`、`distilledReceiptBytes/Authority`）。真实 API 恒 `COMPARISON_AUTHORITY_INVALID`。 |
| **证据** | 调用点无 `sourceReceiptBytes` 等。亲跑：`compareSemanticReplayReceipts({pair, two grants})` → `reason: COMPARISON_AUTHORITY_INVALID`。金牌 `orchestrator.zero-sut` 的 mock **只校验 3 键**，不校验 receipt bytes/authority，可「全绿」。 |
| **建议** | 按 `createSemanticReplayReceipt` 已返回的 `sourceReceipt.bytes/authority` 与 `receipt.bytes/authority` 补齐 7 键；金牌 mock 改为 exactKeys + 四绑定一致，并加一条**静态/源码**钉死生产调用点。 |

### C2 · 生产 canonical 三处 seam 未实现/未绑定：reset、formal prepared-run claim/preflight

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/runtime-cycle-adapter.mjs:125-127,251-254`；`lib/replay/prepared-run.mjs:59-62`；`lib/dual-replay/run-authority.mjs:50-74` |
| **问题** | `canonicalRuntimeCycleAdapter = createRuntimeCycleAdapter({})`：① `resetIssuer.verifyReset` → `deps.verifyReset` **不是函数**；② `inspectClaimedReplayRuntime` / `runCanonicalPreflight` **undefined**，`runPreparedReplay` 恒 `RUN_COMPLETION_INVALID`。仓内 **无** 生产 `verifyReset` / `inspectClaimedReplayRuntime` / `runCanonicalPreflight` 实现（仅金牌注入）。23 步闭环在 **source.reset** 即死，distilled formal 亦死。 |
| **证据** | 亲跑：`canonicalRuntimeCycleAdapter.resetIssuer.verifyReset(...)` → `TypeError: deps.verifyReset is not a function`；`createPreparedRun({}).runPreparedReplay(五枚 opaque)` → `{"ok":false,"reason":"RUN_COMPLETION_INVALID"}`。`rg` 仅命中 adapter 传 deps 与金牌夹具。 |
| **建议** | 在 runtime seams / bootstrap 落地 **canonical** `verifyReset`（闭合 reset receipt + baseline）与 claimed-runtime inspect + preflight；adapter 静态绑定，禁止生产依赖 `deps={}` 空壳。金牌除注入路径外，加 **production-binding** 静态断言：三函数在 canonical 图上为可调用真实现。 |

> C1+C2 合看：gate/金牌「绿」主要靠注入替身与宽松 mock，**生产 façade 不能走完 plan §3.5 / §7 闭环**——属风险清单第 7 条（倒着裁剪迎合金牌）与第 6 条（生产静态咬合）的实锤。

---

## High

### H1 · entity admission 失败 reason 白名单外落 runtime 通道；注入 seam 可铸权

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/entity-lock-verifier.mjs:24-32,198-222`；对照 `lib/entity-semantic-lock-preflight.mjs:846-852` |
| **问题** | `admission.ok !== true` 时仅 `MALFORMED_ADMISSION_REASONS` 内 reason 首错拒绝。`REPLAY_EVENT_POLICY_INVALID`、`REPLAY_READ_EVENT_TARGET_INVALID` 等不在集合 → 落入 runtime-required。`verifyRuntimeEntityLock` seam 下可继续 `mintHandle`。 |
| **证据** | 亲跑：admit 固定 `REPLAY_EVENT_POLICY_INVALID` + 无 authority → `ENTITY_LOCK_RUNTIME_AUTHORITY_REQUIRED`（丢原因）。同 admit + 注入 `verifyEntityLockSet` 授权 → `ok:true, mode:runtime-required, hasHandle:true`。 |
| **建议** | 默认：**admission 非 ok 一律 denied(原 reason)**；仅显式「需锁」类 reason 进 runtime。枚举与 preflight 同源导出，禁止手抄漂移。 |

### H2 · `projectTopologyAuthority` 在 active 空时回退裸 Playwright `page`

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/runtime-owner.mjs:32-38` |
| **问题** | `controller.activePageAuthority()` 缺席或 falsy 时返回装配时的真实 `page`。持有 topology 投影者可拿裸 Page（`url`/`evaluate`/`goto`），绕过 evaluate/perform 门。 |
| **证据** | 亲跑：`activePageAuthority: () => null` + bare page → `fallback is bare page? true`。现役 controller 在无 active 时返回 `null`（`controller.mjs:510`），故回退**可达**。 |
| **建议** | 无 active 一律 `null`/稳定 reason；**禁止** page 回退。调用方（raw driver/runner）对 null 首错停。 |

### H3 · `bin/record.mjs --cycle-plan` 无金牌、形状/路径弱校验

| 项 | 内容 |
|---|---|
| **file:line** | `bin/record.mjs:88-131,326-336` |
| **问题** | S6 新增 CLI 面：`buildCycleInput` 仅 try/catch 折叠为 `CYCLE_PLAN_INVALID`；缺字段、任意路径 `readFileSync`、无 schema exactKeys；**无**对应金牌钉死。与「完成是退出码」纪律冲突：半成品计划静默拖到后段。 |
| **证据** | 源码形态；任务书已标重点；CLI 排除集含 `teachin-cycle`，但 `--cycle-plan` 挂在 `record` 上。 |
| **建议** | exact 计划 schema + 字节路径白名单/存在性；零 SUT 金牌覆盖缺键/坏路径/成功装配键集合；失败 reason 稳定、不回显路径内容（现状 catch 尚可）。 |

---

## Medium

### M1 · verdict 完整性校验与 exec 之间 TOCTOU

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/verdict-cli-adapter.mjs:95-111,196-210` |
| **问题** | `readJudgeBytes` 做 sha256 后，`execFile` 再读磁盘 `JUDGE_PATH`；校验与执行非同一字节镜像。 |
| **证据** | 代码路径分离；本地同用户威胁模型下可替换。锚值本身与现役 `bin/verdict.mjs` 一致（亲跑 sha256）。 |
| **建议** | 校验后写只读临时副本再 exec，或 `node --eval`/stdin 喂已校验 buffer（在 shell:false 约束内）。 |

### M2 · raw-axes 先双消费 authority 再做 binding 校验

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/raw-axes-adapter.mjs:215-242` |
| **问题** | 先消费 raw observation 与 resolved projection，最后才 `bindingsAgree`；探针/错绑烧掉 genuine one-shot。与 resolved-projection「探针不得吃 genuine」取向相反。 |
| **证据** | 源码顺序；失败返回 `RAW_AXES_BINDING_MISMATCH` 时两侧已 consumed。 |
| **建议** | 非消费 inspect 校验 binding → 再原子双消费；或绑定失败时不得标记 consumed。 |

### M3 · `openRunExecution` 非一次性，可重复开箱 runtime 三件套

| 项 | 内容 |
|---|---|
| **file:line** | `lib/dual-replay/replay-completion.mjs:309-328`；返回面 `raw-event-observation.mjs:400-407` |
| **问题** | 无 consumed；同 `runExecutionAuthority` 可多次取 fresh/topology/artifacts。现生产 issuer 单次用，但 authority 若泄漏则可复开。 |
| **证据** | `EXECUTION_STATE.get` 无置位；issuer 把 authority 原样放回 result（completion 层有 `executed` 防重跑，但不防 open 复用）。 |
| **建议** | 首次 open 置 consumed；或仅在 issuer 闭包内 open、永不返回 authority。 |

### M4 · 录制 browser disconnected 监听为恒等 no-op

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/replayability-cycle-entry.mjs:87`（约） |
| **问题** | `contextClosed = contextClosed \|\| false` 不更新状态；finally 主要靠 context close + `isConnected`。browser 已断但 context close 未观察到时，清理分支可能偏离「恰关一次」。 |
| **证据** | 源码恒等；清理仍依赖 `stillLive()`/`contextClosed`。 |
| **建议** | 与 fresh-runtime 一致：disconnected → 置位；或删除死监听并文档化仅 context+isConnected。 |

### M5 · `createRoleAxesSeam` 注入 judge 可把任意对象 JSON 化成「verdictBytes」

| 项 | 内容 |
|---|---|
| **file:line** | `lib/teachin/raw-axes-adapter.mjs:346-360` |
| **问题** | 注入路径下非 Buffer 返回可被 `JSON.stringify` 包装为 ok 判决字节。生产 canonical 不走注入；零 SUT/错误装配可洗绿。 |
| **证据** | `ok: true, verdictBytes: Buffer.from(JSON.stringify(judged))` 无 `verdictMatchesInput`。 |
| **建议** | 注入路径也必须闭合形状 + 与 axes identity 绑定，或禁止非 Buffer 成功。 |

---

## 已扫未升格（简述）

| 面 | 结论 |
|---|---|
| WeakMap authority 克隆/JSON | fresh/action/receipt 等 plain/clone 拒；action 换绑消耗 — 主体扎实 |
| 裁判纯度 | 纯核心无 LLM；production 走 frozen child-process；不 import 裁判逻辑 |
| comparator 语义 | 首错序 + 非 PASS 禁晋升 + role/namespace/predecessor/reset 碰撞拒 — 实现与设计一致（**但生产调不到**，见 C1） |
| 公开 receipt | digest 向；无 URL/locator/session |
| 环境探测迎合 | 白名单内无 `process.env`/`NODE_ENV`/fixture 分支 |
| 600 行门 | 通过；cycle-adapter 295 满足 Y5 |

---

## 终判

**CHANGES_REQUIRED**

阻断项：**C1（compare 生产入参残缺 + 金牌漏钉）**、**C2（reset / prepared formal 生产 seam 空心）**。  
建议同轮处理 **H1–H3**；M1–M5 可随收口修，不单独阻塞若 C 已修且有对应金牌棘轮。
