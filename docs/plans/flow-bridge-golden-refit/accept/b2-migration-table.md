# B2 三列账审计件 —— record-distill.golden.mjs（accept 冻结稿）

> 起草：sonnet-5 只读子代理（2026-07-21）；定稿收录：fable 主环。全部「新拒因」为静态追踪强预测，标〖待实测定谳〗处按 plan 纪律在 loop 阶段红基线实录后冻。
> 段数定谳：**14 段**（文件注释边界切分，与 plan 列举 checkId 集合精确对应：保留 11 + 迁移 3）；字母级子断言 33。早期「17」为粗估、无任何文档逐条列出过，据本审计件订正。

# B2 三列账审计件（草稿）—— `tests/_golden/record-distill.golden.mjs`\n\n> 契约：flow-bridge-golden-refit（full）。本稿严格只读起草，未运行任何金牌/夹具/SUT/gate（遵 SKILL.md:107 与本次任务约束）。全部「新拒因」结论均基于**静态代码追踪**（逐层读 import/调用链），凡未见 `docs/plans/flow-bridge-golden-refit/accept/red-baselines.json` 中有对应实录支撑的，标注「待实测定谳」，不冒充已冻结事实。\n\n## 0. 对象与分段口径说明（先处理「17」与实际清点的落差）\n\n对 `tests/_golden/record-distill.golden.mjs`（234 行）逐字过了一遍，用文件自身的注释边界（`── X ──` 大注 + 归属于同一 `{ }` 作用域的子块）切分，数出的是 **14 个顶层 check 段**，与 `plan.md:43-45` 自己描述性列举所指向的 checkId 集合**完全一致**（保留 11 个 + 迁移 3 个 = 14）：\n\n| # | checkId | 源码 span |\n|---|---|---|\n|1| C1（门面+用法错） | 51-64 |\n|2| C3（happy 投影） | 66-107 |\n|3| C5（降权硬不变量） | 109-115 |\n|4| C6（输出卫生） | 117-119 |\n|5| C2a（NOT_INTAKEN） | 122-131 |\n|6| C2b（CAPTURE_SWAPPED） | 132-139 |\n|7| C2c（软链拒） | 140-148 |\n|8| C2d（REREVIEW_FAILED） | 149-159 |\n|9| C4（采集忠实闸） | 161-175 |\n|10| C2f（编码凭据穿输出门 PoC） | 177-189 |\n|11| C2h（超深编码凭据不收敛） | 190-204 |\n|12| C2g（写盘错误捕获） | 205-215 |\n|13| C4f（--verify 复用 capture 硬门） | 216-226 |\n|14| C7（门面回归） | 228-229 |\n\n`plan.md:42`、`review/sol-consensus-reply-r2.md:15`、`review/sol-consensus-request-r2.md:9` 三处都写「17」，但我按**任何**一致的切分粒度重算都对不上 17：把每个字母级子断言各算一行（如 C1a-g、C3a-g……）得 33 行；把大注块原样合并得 14 行；只放大 C1/C3 两个复合块各拆 2 行也只能凑到 15-16。**没有找到任何文档把这 17 行逐条列出过**——这份三列账本身就是那份从未存在过的清单，「17」目前看是早期粗估数字。已在下方「不确定项」第 1 条挂起，不擅自拍板为 14 或 17。\n\n以下主表按我数出的 14 行铺开；如最终判定应为 17 行，只需在「不确定项 §1」给出的候选拆法里选一种展开即可，不影响每行的归列结论。\n\n---\n\n## 1. 主表（保留 / 迁移，14 行）\n\n| checkId | sourceSpan | 原断言一句话（防的假绿） | 归列 | 归列依据（新执法点 file:line） |\n|---|---|---|---|---|\n| **C1** 门面+用法错 | 51-64 | help 暴露 `casey distill` 且不留过时 `--sut <url>` 黑名单形态；缺 caseId/--capture/--out-dir、裸 `--capture`、caseId 穿越/斜杠/凭据关键词分别精确 exit 64/65——防用法校验或 help 面回归悄悄松口 | **保留** | `bin/distill.mjs:36-43` 全部用法/caseId 校验在任何三件套/authority 逻辑之前执行（package 逻辑起于 :50），与 seamAccepted() 改形无关；C1c-f 只是借 `cap` 做后续调用的合法路径样本，样本怎么产生不影响这段断言本身 |\n| **C3** happy 投影 | 66-107 | 真接缝 happy 恰产 3 候选、无正式回放产物；候选骨架（schemaVersion/caseId/source.kind/uniquePrefix/1:1 steps/intentId=i+seq/无 expected·target）；actionHint 忠实映射且不臆造；pending 全 route:human 且与 manifest.pending 交叉一致；manifest 溯源三方 sha 一致、projection 全覆盖且 pathHint 干净；候选真过 `ingest`；两次 distill 字节确定性一致——防投影残缺/说谎/不可复现/不可二次消费 | **保留** | `bin/distill.mjs:144-145` `projectCapture(reviewDoc)` 只在通过 :102-124 的新 authority 门之后才跑，产出逻辑本体即 `lib/record-distill.mjs` 的 `projectCapture`/`buildDistillManifest`，未因换链路而变 |\n| **C5** 降权硬不变量 | 109-115 | 候选全文无 `signed:true`/`replayReady:true`；候选 mapping 是裸数组；manifest 降权 note 在场；候选 `source` 无 signed/replayReady 键——防候选被误当已签/可直接回放 | **保留** | 同上，纯粹是 `projectCapture` 输出形态断言，与拿到「已 accept 的三件套」的方式（真 record 产出 or 手造）无关 |\n| **C6** 输出卫生 | 117-119 | 候选全文无 `://`；成功 stdout 不回显 out-dir 绝对路径——防候选/日志泄漏 URL 或本机路径 | **保留** | `bin/distill.mjs:178-179` console.log 文案不含 `outDir`；候选文本卫生仍来自 `products` 写盘前的凭据/解码门（:159-168），未变 |\n| **C2a** NOT_INTAKEN | 122-131 | record 后跳过 intake，直接 distill 必 exit 65、零候选落盘——防「未入账」旁路蒸馏 | **保留**（同一拒因字符串） | `lib/teachin-observation-authority-root.mjs:712` `if (!ledgerState.snapshot) throw new TypeError('NOT_INTAKEN')`；即使走到 `verifyIntakenPackage`，其 `lib/record-distill.mjs:79` 无 accepted 条目时同样回 `'NOT_INTAKEN'`。旧的单文件版 `verifyIntaken`（`lib/record-distill.mjs:11-23`）该文件自己在 `:49-50` 注明「新 distill CLI 不再调用它」——已确认为死代码，实际执法点已迁到 `verifyIntakenPackage`/`rehydrateAcceptedObservationTransaction`，但**回的拒因字面量没变**。新增前提：手造 fixture 现在必须先备出一套能通过 `readPackage`（authority-root.mjs:707）识别的合法三件套（capture+sidecar+manifest），再让 ledger 缺失，才能落到这个分支——不再是「裸 capture 直接跑」 |\n| **C2b** CAPTURE_SWAPPED | 132-139 | intake accept 后篡改 capture 字节，distill 须 exit 65、零候选——防「哈希对不上仍蒸馏」的 TOCTOU 缺口 | **保留**（同一拒因字符串） | `lib/record-distill.mjs:89` `if (latest.captureSha256 !== currentCaptureSha256) return {ok:false, reason:'CAPTURE_SWAPPED'}`，由 `lib/teachin-observation-authority-root.mjs:713/723` 的 `verifyIntakenPackage` 调用链路承接，字面量与旧版一致 |\n| **C2c** 软链拒 | 140-148 | `--capture` 是符号链接须 exit 65、不跟随——防软链换址攻击 | **保留** | `bin/distill.mjs:56-59`（`lstatSync(...).isSymbolicLink()`）是 CLI 自身预读守卫，位于任何 package/authority 代码（:62 起）之前，结构上不受 seam 改形影响 |\n| **C2d** REREVIEW_FAILED | 149-159 | 手造一份 sha 与 accept 台账精确匹配、但**内容本身**不该过复核的 capture（`startPath` 是完整 URL），distill 须 exit 65——防「台账哈希对得上就直通」的复核缺口 | **迁移** | 见 §2 详表 |\n| **C4** 采集忠实闸 | 161-175 | `--verify --mapping`：空 mapping（v1 全 pending）合法 exit 0；凭空 intentId exit 65；mapping 非数组须 fail-closed exit 65（不崩）——防闸门抛异常或纵容伪造 mapping | **保留** | `bin/distill.mjs:148-156` 的 `--verify` 分支在 :102-124 新门之后才跑，`validateCaptureFidelity`（`lib/record-distill.mjs`）逻辑未动 |\n| **C2f** 编码凭据穿输出门（PoC） | 177-189 | 单层 `%HH` 编码的 \"password=secret\" + 哈希匹配但版本/台账可能漂移的旧式 ledger，distill 仍须非 0 拒绝、零候选——防「旧台账/版本漂移」绕过输出凭据门 | **迁移** | 见 §2 详表 |\n| **C2h** 超深编码凭据不收敛 | 190-204 | 21 层编码（超 `normConverge` 20 轮 cap）不收敛须在**距离蒸馏最近的凭据门** fail-closed，钉死 `exit 1` + stderr 命中「不收敛」——防「解码不到底、关键词扫不到」假阴性 | **迁移** | 见 §2 详表 |\n| **C2g** 写盘错误捕获 | 205-215 | 第二产物路径被目录占位 → exit 1 errno、stderr 不回显 out-dir、第一份候选（testcase）不残留半份——防写盘失败留脏产物 | **保留** | `bin/distill.mjs:169-176` 写盘+清理逻辑是 happy path 末段，只有先过 :102-124 新门才能到达，逻辑本体未变 |\n| **C4f** --verify 复用 capture 硬门 | 216-226 | 干净 distill 后篡改 capture，同一份（旧）mapping 再 `--verify` 须 exit 65——防 `--verify` 信任可变 manifest 而非重跑 TOCTOU | **保留** | `bin/distill.mjs:144-153` 每次调用（含 --verify）都从当前读入的 `doc` 重新 `projectCapture`；到达这里同样先决于 :102-124 新门在**当前（篡改后）字节**上重新求值，TOCTOU 语义闭合未变，只是判定入口换了新门 |\n| **C7** 门面回归 | 228-229 | `casey selftest --tier1` 无回归——防新增 distill 面破坏确定性内核自检 | **保留** | 与 seamAccepted()/authority 链路完全无关的独立命令 |\n\n---\n\n## 2. 迁移三行详表（旧拒因 → 新拒因候选，出处 file:line）\n\n### C2d（REREVIEW_FAILED，149-159）\n\n- **旧拒因**：`bin/distill.mjs:142` `` `REREVIEW_FAILED:${review.reason}` ``（本地 `reviewCapture` 重跑，`review.reason` 来自 `lib/record-intake.mjs` 对 `startPath` 的 `URL_LEAK` 判据，`lib/record-intake.mjs:139` `if (!isPathOnly(doc.startPath)) return rej('URL_LEAK')`）。\n- **新拒因候选**：`CAPTURE_URL_LEAK`，exit 65。\n  - 出处链：`bin/distill.mjs:102` 先调 `rehydrateAcceptedObservationTransaction` → `lib/teachin-observation-authority-root.mjs:707` `readPackage(...)` → `lib/teachin-observation-authority-root.mjs:239` 调 `reviewClosedIdentityObservationPackage(...)` → 该函数内部 `lib/teachin-identity-package-validator.mjs:199-202`：\n    ```\n    const base = { ...capture, schemaVersion: 1 };\n    delete base.identityObservations;\n    const captureReview = reviewCapture(base, { caseId });\n    if (!captureReview.ok) fail(`CAPTURE_${captureReview.reason}`);\n    ```\n    对同样携带完整 URL 的 `startPath`，`reviewCapture` 仍会命中 `URL_LEAK`（`lib/record-intake.mjs:139`），组合成 `CAPTURE_URL_LEAK`，在 `bin/distill.mjs:102-106` 处以 exit 65 抛出——**先于** `bin/distill.mjs:141-142` 本地那次重跑。\n  - sol 侧同一预核：`review/sol-consensus-reply-r2.md:20`、`review/sol-consensus-reply-r3.md:28`。\n  - **⚠️ 待实测定谳**：`docs/plans/flow-bridge-golden-refit/accept/red-baselines.json:34-39` 里 `record-distill` 条目 `exit:1` 但 `keyLines:[]`（空），说明目前的红基线空跑**没有捕获到具体 stderr**——上述推导是静态追踪结果，plan.md:44/72 与 sol 回复都只到「预核/可行」层级，尚无一次针对 C2d 单独输入的实录来钉死 `CAPTURE_URL_LEAK` 就是最终吐出的字符串（比如 `readPackage` 之前的 `verifyTeachInPackage`/软链/布局检查是否会更早拦截，需要拿新版三件套夹具真正跑一遍才能确认）。\n\n### C2f（编码凭据穿输出门 PoC，177-189）／ C2h（超深不收敛，190-204）\n\n- **旧拒因**：\n  - C2f：`bin/distill.mjs:135` 本地 canon+解码门，`assert(r.status !== 0, ...)`（断言本身宽口径，只要求非 0）。\n  - C2h：同一段代码，钉死 `exit 1` + `/不收敛/`（`bin/distill.mjs:135` 的 `console.error('...编码凭据/超深不收敛编码...')`）。\n- **新拒因候选**：`CRED_GATE_HIT`，exit 65。\n  - 出处链：同 C2d 走到 `lib/teachin-identity-package-validator.mjs:167`（`reviewClosedIdentityObservationPackage` 函数体）→ `:178-182`：\n    ```\n    const canonicalCapture = JSON.stringify(capture);\n    const decoded = normConverge(canonicalCapture);\n    if (!decoded.converged\n      || !credentialGate({ capture: canonicalCapture }).ok\n      || !credentialGate({ capture: decoded.s }).ok) fail('CRED_GATE_HIT');\n    ```\n    - C2f 场景：单层 `%HH` 解码一轮即收敛（`decoded.converged===true`），但解码后文本含字面 `password`，`credentialGate` 命中 `FORBIDDEN_KEYWORDS`（`lib/cred-gate.mjs:9` 含 `'password'`）→ `CRED_GATE_HIT`。\n    - C2h 场景：21 层编码超过 `normConverge` 20 轮 cap（`lib/record-intake.mjs:52-59`，`for (let i=0;i<20;i++)`），`decoded.converged===false` 直接命中同一 `fail('CRED_GATE_HIT')`。\n  - 我逐行比对过：`teachin-identity-package-validator.mjs:178-182` 与 `bin/distill.mjs:130-135` 两段 canon/decode/credentialGate 逻辑**在相同输入下判定完全等价**（都是 `JSON.stringify(同一份已解析 capture)` → `normConverge` → `credentialGate`），且前者必然先执行（`bin/distill.mjs:102` 早于 :130）。也就是说：**只要能走到 `bin/distill.mjs:130`，说明 :102 的门早就该拦下同样的输入而没拦下**——在真实 CLI 路径下，这两个用例原本要测的「distill 自身本地门生效」这件事，现在结构性地测不到了（见 §3 丢失项 4）。\n  - 直调红证的可行性核实（`lib/teachin-identity-package-validator.mjs` 全部导出）：\n    ```\n    export function validateClosedIdentityObservationSidecarDocument(value, { caseId } = {})   // :139\n    export function reviewClosedIdentityObservationPackage(options = {})                         // :167\n    ```\n    仅此两个导出，无第三个。`reviewClosedIdentityObservationPackage({ caseId, captureRaw, observationRaw })` 是纯函数（文件头 :2 自注「纯函数、零 I/O」），且其内部对 `captureRaw` 的 canon/decode 检查（:175-182）**先于**对 `observationRaw` 的 sidecar 校验（:183-186）执行——意味着直调红证甚至不需要构造一份合法 sidecar，只要 `captureRaw` 命中 `CRED_GATE_HIT` 就会在触及 `observationRaw` 之前抛出，红证输入可以很薄。\n  - **⚠️ 待实测定谳**：同 C2d，`accept/red-baselines.json` 未留下这两条的独立 stderr 实录；`reviewClosedIdentityObservationPackage` 返回值确认是 `{ok:false, reason:'CRED_GATE_HIT'}`（catch 块 `lib/teachin-identity-package-validator.mjs:229-234` 对 `error.message` 做 `/^[A-Z][A-Z0-9_]*$/` 正则透传），这一步是纯代码阅读可证的（无需实跑），但「经由 CLI 整链真的会先落到这里、而不是被更早的布局/软链/台账 shape 检查拦下」仍需红基线实跑冻结。\n\n---\n\n## 3. 丢失（挂账 `record-three-piece-producer`）\n\n以下 4 项**不对应现有任何一个 checkId**（现有 14 行全部落在「保留」或「迁移」），是 `seamAccepted()` 从「真 `record` → 真 `intake`」改形为「手造三件套 → 真 `intake` → 真 `distill`」之后，**结构性**测不到的能力/覆盖面，故单列挂账，不强行塞进某一行假装已迁移：\n\n| 丢失项 | 丢失断言/能力原文 file:line | 接手条件 | route |\n|---|---|---|---|\n| ① record producer 段真 CLI 产出 | `bin/record.mjs:6,48-50`：`writePackage` 只调 `writeTeachInCapture`（`lib/record-capture.mjs`）写单一 `teach-in-capture.json`，**不产** `identity-observations.json`/`teach-in-package.json`。当前 `seamAccepted()`（`record-distill.golden.mjs:34-45`）用真 `record` CLI 只能拿到「capture 单件」，另两件套仍须手造 | 后续契约扩 `bin/record.mjs` 真实产出完整三件套，再让 `seamAccepted()` 三件全部改回真 CLI 产出 | machine（纯代码扩展，非需人裁的语义问题） |\n| ② record→intake 字节接缝 | `record-distill.golden.mjs:38-42`（现状）：`record` 产出的 capture 字节被**原样直接**喂给 `intake`，无手造插手。改形后 capture 字节本身也要参与手造/拼装，「record 真实产出的字节原封不动过 intake」这条端到端接缝不再被验证 | 同①，producer 契约把 record→intake 的真实字节路径接回来后即可恢复 | machine |\n| ③ 旧单文件布局兼容 | 隐性能力：05573d1/edea1f9 之前，裸 `<caseId>/record-capture/teach-in-capture.json`（无 sidecar/manifest 同级）是合法 intake/distill 输入；现在 `bin/intake.mjs:61-72`、`bin/distill.mjs:62-74` 均硬性要求两份同级旁车文件存在且非软链，否则 exit 65/1（「示教三件套缺 xxx」）。当前文件里没有任何 check 显式断言过「旧单文件布局」这条能力，之前是隐性成立、现在隐性作废 | 若未来真要一条「旧单文件布局显式判负例」的负向断言，需另立/并入 producer 契约；否则维持隐性作废、不补断言 | machine |\n| ④ distill 内联本地编码门的行为级覆盖 | `bin/distill.mjs:130`（注释起点）/`:131-135`（实体代码）：canon+解码+credentialGate 序列内联在 `main()` 里，`main()` 未导出，且模块底部 `bin/distill.mjs:183` `main();` 无 `import.meta.url` 门禁、**import 即执行**，无法安全直调。逐行比对确认它与 `lib/teachin-identity-package-validator.mjs:178-182` 判定逻辑等价、且后者经 `bin/distill.mjs:102` 恒先执行——C2f/C2h 原本借真实 CLI 调用顺带验证的「这段本地代码自己也真的会执行到并生效」这件事，现在结构性不可达 | plan.md:45 原文接手条件：后续契约把该门拆成可导出纯函数（如 `validateDistillEncodingGate(doc)`）并让 `main()` 调用它；或与 record-three-piece-producer 契约一并处置 | machine |\n\n---\n\n## 4. 不确定项（明说卡点，不假称确定）\n\n1. **「17 段 check」与我数出的 14 段对不齐，且找不到任何文档逐条列出过这 17 行。** `plan.md:42`、`sol-consensus-reply-r2.md:15`、`sol-consensus-request-r2.md:9` 三处反复提「17」，但均是叙述性提法，没有配套清单。我用文件自身的注释边界切分得 14（与 plan.md:43-45 描述性列举的 checkId 集合精确对应：保留 11 + 迁移 3）；若改用「每个字母子断言各算一行」的粒度重切，总数是 33（C1:7/C2:7/C3:7/C4:4/C5:4/C6:2/C7:1），也不是 17。介于两者之间的任何「只拆某几块」的折中方案都能凑出 15/16/18 等数字，但没有一种是唯一自然的。**这份草稿按 14 行铺开；若你判定必须是 17 行，需要先决定具体是哪 3 行需要再拆（我倾向 C1 拆「help 静态检查」vs「cap 依赖的用法错」2 行、C3 拆出 C3e「真过 ingest」单独 1 行，凑够 17，但这只是我的建议拆法，非既定事实）。**\n2. **C2d/C2f/C2h 的新拒因是静态代码追踪结果，非实测定谳。** `accept/red-baselines.json:34-39` 里 `record-distill` 一条 `exit:1, keyLines:[]`——红基线空跑捕获了退出码但没留 stderr 摘要，无法从这份文件反推这三条各自的真实拒因字符串。§2 里给出的 `CAPTURE_URL_LEAK`/`CRED_GATE_HIT` 是我逐层读 `bin/distill.mjs` → `teachin-observation-authority-root.mjs` → `teachin-identity-package-validator.mjs` → `record-intake.mjs`/`cred-gate.mjs` 调用链后得出的强预测（且与 sol 三轮共识 `reply-r2:20`、`reply-r3:28` 的判断一致），但按 GRILL/plan 自己的纪律（plan.md:72「C2d 实测拒因定谳实录」），这仍需 accept-gate 阶段真跑一次冻结，我不能替代这一步，也不该在此稿里把它写成已冻结事实。\n3. **改形后各 check 的确切 sourceSpan 会变，我给的是「现状」spans，不是「改后」spans。** plan.md:32 的「租约卫生」规则（每份租约进 try/finally）意味着重写后的 `seamAccepted()`/各手造三件套块很可能需要各自独立的 try/finally 包裹（不再是整文件一个大 `try{...}finally{rmSync(tmp)}`，参照 `observation-cli-authority-wiring.zero-sut.golden.mjs:184` 先例），行号边界会整体挪动。本表的 sourceSpan 只用于定位「现在防的是什么」，不能直接当「改后应该长这样」的规格。\n4. **C2a/C2b 判定为「保留」的前提——新版手造三件套下这两个负向 case 具体怎么构造——我没有去设计。** 我只确认了「NOT_INTAKEN」「CAPTURE_SWAPPED」两个拒因字符串在新链路（`verifyIntakenPackage`/`rehydrateAcceptedObservationTransaction`）里依然原样存在，且能推出「必须先有合法三件套才能落到这两个分支」这个新前提；但具体 fixture 怎么用「租约固定根+动态签」拼出一份「合法三件套但故意跳过 intake / intake 后篡改 capture」的场景，属于实现细节，本稿不代做。\n5. **`tests/_golden/support/ephemeral-driver-publication.mjs`（plan.md:30 提到的新增 support 件）目前尚不存在**（我核对了 `tests/_golden/support/` 目录，只有 `canonical-case-lease.mjs`、`refit-regate-verify.mjs`、`resign-changed-goldens.mjs`、`retry-transient-fs.mjs`、`sut-startup-closure.mjs` 五个既有件）。这符合「accept 阶段只出审计件、不动生产/夹具代码」的预期，但意味着 §2 里「直调闭合 validator 纯函数」的红证脚本、以及 B2 改形后的实际测试代码，目前都还没有对应文件可比对——本稿的所有「归列依据」都是对**生产侧 `lib/`、`bin/` 现状代码**的静态核实，不是对「已重写的金牌」的核实（因为它还不存在）。
