# learn — teachin-cycle-axes-atom

- resolved mapping 的 atom 是 StepAxes identity 的权威来源；raw action 只是浏览器动作，
  不能替代 canonical atom。
- adapter 接缝金牌必须同时真驱 projector 与 frozen verdict CLI，否则“judge exit 0、
  output binding 拒绝”的生产失败会被 verdict 替身遮住。
- structural event 必须继承其触发 mapping atom，并由金牌逐行冻结；只测 business tail
  不足以守住复合事件。
- 真机拒付从 axes 投影推进到 verdict identity，证明前一修复生效，但仍不是完成；
  只有最终码 cycle exit 0 才核销 route:human。
