# learn · terminal-coverage-yield

1. **裁定表述与可实现判据要当场对齐**。Steven 点选的「同流同 platformId 观察在场才
   豁免」在成品层不可直译（让位行从未归档、无其 platformId 可比）——把偏差如实摆回
   （目标名桥接 vs platformId 硬桥接）再裁，落成「事件取证字段 + 门内重推导」的形：
   字段是证据不是宣告，门自证恰一匹配行。裁定语义保真、fail-closed 不松。
2. **「取证记录非豁免宣告」是让位类豁免的通用形**：伪造字段无匹配行照拒（Y3）、
   义务面白名单（恰 `['source']`，Y5）、恰一匹配（Y4）、只对「少=0」开（Y6 多面
   由去重钉关死）——四条件 AND 使豁免不可组合滥用，评审双方独立未找到第五条泄漏。
3. **加法门控让冻结邻接零改版**：豁免以新字段门控，无字段输入行为逐字节同码——
   cardinality-reverse / wiring / observation-yield 三家冻结金牌零触碰全绿，冻结面
   改动只剩 schema 一件（本就挂账待改）。比「改判据本体」省一整圈改版重签。
4. **挂账同车兑现的正确时机**：M1 schema pattern 与新属性同一次改版、同一次人签
   （Steven 原裁「与基数门修向统一处置」），两份归属 PRD 的 `checksumAmendments`
   一次登记——挂账不过夜、签字不碎片化。
5. **首轮双 APPROVE 的工艺配方**（三契约演进的收敛）：裁定链全录进 GRILL → 金牌
   红面即契约面 + 遗留等价钉 → 突变整体还原闭环 → ext4 克隆树评审（评审方自跑
   验证）→ 风险清单先行自证伪。评审方只剩补测与文案笔误可挑。
6. **挂账**（承前追加）：plan.md 冻结字节「三份」笔误（随下次触碰顺手清）；teach-in
   三通道与 draft 闸面承前；后两例 `publish_states`/`history_version` 待 crud 全绿。
