# grill 记录——semantic-lock-cert-closure（2026-07-17）

## 决策树与清空过程

1. **这活该不该做、谁来做？** 三路只读核账（现场实证 / P0 处置取证 / Claude 攻击式评审）确认：v2 防御代码闭合但认证全线漂移，两个 prd `passes:true` 与实跑 gate RED 不符——合并前硬门。Steven 以可点选项确认「停 codex、Claude 接管」，并确认先抢救提交两处 `/tmp` 未提交特性（已落 `902216e`、`02f4361`）。
2. **修测试还是修实现？** 评审证实实现无 Critical/High/Medium 缺陷，红因是测试按旧 API 形态构造夹具 + publications 表被故意清空。故本契约只修测试夹具构造与认证账，不动实现；若过程中发现实现真 bug，停下上报（见 plan「不做」）。
3. **publications 表要不要回填？** 不回填。清空是刻意安全姿态（冻结测试 digest 不是生产出处），测试走仓内既有测试专用权威注入手法，生产面保持 fail-closed。
4. **孤儿测试直接删吗？** 不直接删。先 git 考古定性（模块曾存在被合并丢 / 从未存在的过早冻结 / 下一波故意红基线），按结论分别恢复、吊销（仓内既有吊销惯例）或保持红。
5. **要不要新登记术语？** 不需要。本契约全部使用既有登记词（语义锁、金牌、gate、冻结、重签、吊销），无造词。

## 结论

入口分流 light 车道成立：范围有界（三金牌重写 + 四孤儿处置 + gate 重证），不触发架构决策，无 ADR 需求。上游档案：主树 `docs/plans/closed-loop-evolution/review-claude-20260717.md`。
