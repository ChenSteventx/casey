precondition-atom-workflow · acceptance-gate 红/绿基线
capturedAt: 2026-07-27
environment: zero-SUT；未启动浏览器；未访问任何 SUT

NEW 1
command: node tests/_golden/precondition-atom-workflow.zero-sut.golden.mjs
exit: 1
failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

NEW 2
command: node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs
exit: 1
failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

NEW 3
command: node tests/_golden/setup-runtime-barrier.zero-sut.golden.mjs
exit: 1
failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

以上三个测试均先通过 node --check，红因是目标实现模块尚不存在，不是测试语法错误。

REGRESSION 1
command: node tests/_golden/intent-state-trace.zero-sut.golden.mjs
exit: 0
output:
ok   intent-state-trace: 5/5（单一状态机 + trace + 旧 problems 字节兼容）

REGRESSION 2
command: node tests/_golden/flow-bridge.golden.mjs
exit: 0
output:
flow-bridge golden: 17 过 / 0 败

REGRESSION 3
command: node tests/_golden/entity-binding-operability-successor.zero-sut.golden.mjs
exit: 0
output:
ok   entity-binding-operability-successor/all: 7/7 全过（本地文件/纯函数/静态，零 SUT）

ACCEPTANCE AMENDMENT R1
reason:
实现主审发现 registry exclusive group 会在 workflow.open 提供「画布已开」时移除
「在工作流管理页」。旧 B1 同时要求两状态进入 receipt，会把已移除的中间态伪装成主体可用初态。

corrected expectation:
setup receipt 只保留 setup 结束时仍在 stateTrace.finalStates、且确由 setup 提供的状态；
中途 provide 后又被移除的状态不得进入 receipt。

red verification base:
57e4130（S1 实现前冻结提交）

command:
node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs

exit:
1

failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

说明：修正版测试已在实现前基线独立 worktree 运行；红因仍为目标模块不存在，不是语法错误。

ACCEPTANCE AMENDMENT R2
reason:
独立实现评审发现 B6 使用了裁剪后的非生产 identity observation：观察行缺现役 compile 必产的
kind/sourcePath，source.kind 也误写成 agent，而生产 issuer 为 compile-envelope。

corrected expectation:
B6 使用现役 identity-observations.compile.json 的完整观察行与 compile-envelope source；
setup admission 必须直接兼容生产观察件，不能只通过自造裁剪夹具。

red verification base:
57e4130（S1 实现前冻结提交）

command:
node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs

exit:
1

failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

说明：R2 修正版同样在实现前基线独立 worktree 运行；先通过 node --check，红因是目标模块不存在。

ACCEPTANCE AMENDMENT R3
reason:
独立实现评审发现四类可达性/真实性缺口：
1. TestCase 业务前置文本被当成运行时已满足，未 probe/readback 也能启动主体；
2. setup plan 未先准入，坏 plan 可能先调用 adapter 再拒绝；
3. 已登记 identity observation 的 setup atom 可省略引用或接受错 kind；
4. session 只绑定 receipt，不绑定本次 adapter 执行；旧 execution evidence 可在 fresh session
   重新 finalize 并再次启动主体。
同轮复核还确认现役 workflow.open 动作角色 subject 与观察角色 source 冲突；不得改夹具掩盖，
也不得擅改人签策略。

corrected expectations:
- 只有 Login Bootstrap 的「已登录」可直接进入 setup initialStates，其他业务前置必须由 atom workflow 取证；
- barrier 在 executeSetup 前完成 plan admission，未就绪 plan 的 setup/main adapter 均零调用；
- observation registry 已登记的 setup atom 必须完整提供对应角色的 ref，issuer/kind/关联键逐项匹配；
- barrier 在执行前签发一次性 execution challenge，evidence 必须回绑；旧 evidence 在 fresh session 拒绝；
- workflow.open 原场景保留，并在角色权威未统一前于规划期具名 SETUP_IDENTITY_ROLE_CONFLICT、
  route:human。

red verification base:
57e4130（S1 实现前冻结提交）

commands:
node tests/_golden/precondition-atom-workflow.zero-sut.golden.mjs
node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs
node tests/_golden/setup-runtime-barrier.zero-sut.golden.mjs

exits:
1
1
1

failure for each:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

说明：三条 R3 当前测试字节均通过 node --check；随后把 import 重定向到 57e4130 独立 worktree
逐条执行，三者均因目标 setup-flow 模块尚不存在真实 RED，不是语法错误或伪造标记红。

ACCEPTANCE AMENDMENT R4
reason:
R2 独立复审发现，仅把业务 precondition 从 initialStates 排除仍不够：candidate 可以完全不把某项业务
前置列入 goal/setup，receipt 也不证明该项，主体却仍启动。这样“未信任”被错误降成了“直接忽略”。

corrected expectations:
- 每个非 Login Bootstrap 的 TestCase precondition 都必须由 candidate goalStates 精确覆盖，并在 setup
  最终 providedStates 中有 post-readback 或 already-satisfied probe；
- 缺 goal 具名 SETUP_PRECONDITION_GOAL_MISSING，缺最终证据具名 SETUP_PRECONDITION_UNVERIFIED；
- 同 case TestCase 后续新增业务前置时，旧 plan/receipt 不能授权主体。

red verification base:
57e4130（S1 实现前冻结提交）

commands:
node tests/_golden/precondition-atom-workflow.zero-sut.golden.mjs
node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs

exits:
1
1

failure for each:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

说明：两条 R4 当前测试字节均先通过 node --check，再以 import 重定向方式在 57e4130 独立
worktree 执行；红因仍为实现前目标模块不存在。

ACCEPTANCE AMENDMENT R5
reason:
challenge 定点审计发现“一次性”只约束成功路径：同一 session 可重复取得 request，失败 finalize 不消费；
不同 run 还会生成字节相同 receipt，文档容易把相关性检查夸成 readback freshness 证明。

corrected expectations:
- 同一 session 只签发一次 execution request，首次 finalize 尝试即消费；
- receipt 必须绑定 execution request digest，不同 session/run 的 receipt 字节不同；
- 明确信任边界：executeSetup 是受信 evidence adapter。S1 防未改写 envelope/receipt 重放，但无签 JSON
  不能证明旧 readback 没被错误 adapter 重包 fresh challenge；该 freshness 接缝 route:human/后继浏览器 UAT。

red verification base:
57e4130（S1 实现前冻结提交）

command:
node tests/_golden/setup-receipt-identity.zero-sut.golden.mjs

exit:
1

failure:
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'lib/adaptive-execution/setup-flow.mjs'

说明：R5 当前测试字节先通过 node --check，再在 57e4130 独立 worktree 真实执行；红因是实现前
目标模块不存在。
