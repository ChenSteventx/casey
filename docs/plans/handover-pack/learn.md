# handover-pack — learn（light，codex 四轮 R4 PASS）

1. **移交文档的凭据卫生要机器锁，不能靠「写的时候小心」**：README 是最容易被后续编辑携进真值/
   真地址的文件——漂移锁金牌用 `FORBIDDEN_KEYWORDS` 单一事实源做键值形态扫描 + 全 URL 回环 host
   断言，把「移交文档零真值零真地址」从写作纪律变成回归红线。
2. **`--sut <url>` 这类措辞本身就是泄漏面**：签名没错、值纪律没写，新同事照着 help 就会把真目标
   地址敲进 shell 历史。接口文档的每个参数占位符都要问「这个措辞会诱导什么值进来」——改
   `<本地基址>` + 一行值纪律注，比事后洗 shell 历史便宜。
3. **评审包摘写省略号是假矛盾之源**：把 sign 用法摘成 `[--signed-at ...]` 让评审员两轮追打
   「help 与基线矛盾」——引权威源要逐字，省略号吞掉的尾巴会变成下一轮的发现。对账后顺藤补全了
   help 与真用法串的完整对齐 + 全旗标锁，坏事变好事。
4. **help 文案修完就该锁**：本仓 help 曾滞后现状三个月量级无人报错（cli-mcp-face 审计发现）；
   这次锚集锁进金牌（过时形态负锚 + 关键旗标正锚 12 项），help 回潮即红。

## 挂账（不静默丢）

- 建远端 / `git bundle` / 凭据带外交付：维护者动作（route:human）；
- README 可读性最终由 Steven 过目定夺（route:human）；
- NEXT-SESSION 刷新归 `/session-handoff`（本 session 收尾时跑）。
