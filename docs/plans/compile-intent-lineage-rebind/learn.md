# learn · compile-intent-lineage-rebind

1. **裁定前提要用代码与探针重验，别信文档转述**。HANDOFF 记「三道冻结门一致表达单流单
   发行」，逐门核代码 + 纯函数探针后证伪三分之二：出处链闸的真拒因是意图号命名空间错配
   （生产 `intent_N` vs 流步 `sourceIntentId`），与发行语义无关。原 A/B/C 岔口据此重摆，
   Steven 改裁「先修接线取真证再裁」——一次勘察省下一整个错方向的契约。
2. **纯函数金牌假绿的新亚型：夹具手工对齐两套命名空间**。`p9-replay-grant-fixtures` 把
   事件 `intentId` 与流步 `sourceIntentId` 写成同名，喂了生产从不产出的理想输入——闸金牌
   全绿而生产必拒。教训并入「accept 金牌须过真缝」：本契约 G1a 用 mock Page 驱真实
   `compileFlow`、G1b 才准手搓字节（钉闸语义本身）。
3. **「从未走通过」家族第五例**：十三跑是史上第一跑抵达成品闸（前十二跑全阻断在执行链
   更早层）——深层闸的缺陷只有链路推进到它才暴露，绿≠验证过，只是未触达。
4. **设计裁定也会被冻结金牌证伪，反转要走完整红绿链**。混合态「全或无掷错」是我推荐、
   Steven 批准的姿态，被 `bindagent-replay` 夹具口径（变异步带号、nav/assert 裸步、assert
   折锚是既定惯例）当场证伪；反转为逐步存在即绑，改版红证与修后绿各自实抓、突变闭环
   重验。全仓双态扫描（护栏 #19）是逮住它的唯一手段——邻接组当时全绿。
5. **异构评审真抓真放**：pi 零 C/H/M 通过的同一快照，grok 逮出 H1 High（重绑漏
   observed/verification 旁路、draft 存在性闸必拒）——emit 时刻写入的投影通道是重绑类改动
   的固有盲区，「三通道逐 stepId 同号」现已入金牌钉。双路家族互补性再实证。
6. **并行扫描互相污染**：双侧全仓扫描同机并行跑，190 项非零大半是抢端口/夹具的对称假红
   （对称性使差异对比侥幸仍有效）。此后全仓扫描单侧串行跑，基线与候选分时段。
7. **drvfs 事故下的评审连续性配方**：D 盘一日四挂，把「commit 快照 → ext4 浅克隆 +
   `loop-kit` 兄弟克隆 → 评审在克隆树自跑验证」固化为姿势——内容寻址使评审地点无关紧要，
   同哈希即同字节；流程对故障盘的依赖压缩到 merge 一瞬。
8. **挂账**（承前追加）：M1 冻结 `events.schema.json:78` intentId pattern 与 authored 号
   张力（Steven 裁十四跑后与基数门修向统一处置、一次人签）；teach-in lineage 块同缺
   observed/verification 重绑（其正式产物若日后过 draft 闸需另开面）。
