# loop-kit-extract round-2 评审逐条处置（汇裁记录，2026-07-13）

> 汇裁者：fable@xhigh（与规划方同族——按自评张力护栏，默认采信异构发现，仅凭具体实据反驳）。评审原文：`planreview-codex-r2.md`（codex-sol@max，含代码级复现与实测）。修订落点：`../plan.md` 与 `../proposed/GRILL.md`（顶部各记一行 round-2 修订）。
> 结论：8 条**全部采信**、零条驳回——逐项复核未找到任何具体反证（`LOOP_KIT_PKG` 跳锁与「异地树必须用它」的耦合、D5 分类矛盾、`breaker --reset` 写实时时间戳、锁先冻结而包仓 commit 后出生，均对照 GRILL/plan/源码逐条核实成立；R2-H3/R2-L1 另有 codex 可复现实证）。R2-H1–H4 与 R2-M1–M3 修入 GRILL/plan；R2-L1 记债（`plan.md` §7）不阻塞。
> 重大变更判定：**否**。全部修订为堵缝加严，未推翻薄 `shim`/`kit-lock`/观测基线机制/D5 加严方向任何一项（codex 附注同此判断：「上述 findings 不重开既定方向」）；Steven 四裁定（有条件签署/D8 取甲/D5 加严/D2 fresh init）均未被触碰。据此 round-2 收口，route:human #1「有条件签署」的条件满足，实现开工闸放行。

## R2-H1 `boot.mjs` 自身装载失败绕过 guard 的 exit 2 归一 —— 采信

- 复核：GRILL D4 原文只写「十个 `shim` 只调 `boot`」，未约定 `boot` 装载失败的边界；C4 故障族确无 `boot` 自身各态。`shim` 若在降级逻辑可达之前因 import `boot` 失败以 exit 1 退出，对只认 2 的 `hook-loop-guard` 即 fail-open——与 round-1 H1 的实证同构，成立。
- 修订：① GRILL D4——`shim` 模板内以最小内联 `try/catch` 边界包住对 `boot` 的动态 import 与调用（边界属模板一部分、C5 逐字钉死），`boot` 缺失/语法损坏/依赖装载失败/锁解析抛错全部落 D5 矩阵；② D5 故障域与三入口行补 `boot` 侧各态、C4 扩格（`boot` 缺失 / `boot` 语法损坏 / 锁 JSON 损坏 / `boot` 调用前中抛错）；③ 按 codex 要求补信任边界明示：`shim` 自身字节损坏在归一保证之外（信任根一员、由 git 与 prd 冻结面护），D5 的「一切引导失败」精确化为「信任根之外的一切引导失败」。plan §1.4/§3 S3/§6 `shim` 词条同步。

## R2-H2 `LOOP_KIT_PKG` 把改定位与关闭校验绑在一起 —— 采信

- 复核：GRILL D2 规定异地树必须显式设 `LOOP_KIT_PKG`，D4 又规定该变量跳过身份锁——一个正常受支持布局即天然运行未验证 `gate`，假 `gate` exit 0 复活 round-1 H2 的循环信任。耦合确认存在，成立。
- 修订：`LOOP_KIT_PKG` 改为**显式定位口**——只改包位置、不豁免校验，两种定位方式一体过同一 `kit-lock` 全清单校验，任何受支持布局零无校验通道；测试需求改走**独立受测锁注入接缝**（`boot` 的锁文件位置参数化，校验逻辑与失配处置一字不变，C2 标记包配对其内容生成的测试锁）。GRILL D2/D4/C2、plan §1.4/§1.5/§3 S2、route:human #6 文本同步。

## R2-H3 单一进程级根探针不能证明缓存命中模块实锚；`finally` 恢复不满足并发零污染 —— 采信（codex 代码实证）

- 复核：codex 最小复现（`A/contract → B/term-lint → B/contract`）证明「import 后核对探针」在交错导入下假绿——探针被后续导入刷新、缓存命中模块仍锚树 A 而核对通过；并发复现证明「保存 env → 设置 → `finally` 恢复」交错下残留污染值。round-1 M2 修订方案被实证击穿，成立。
- 修订（取 codex 给出的「取消进程环境突变」分支，不取全局串行化——更简单且同时杀死两类复现）：① `lib/root.mjs` 契约改为**原子认领**——解析成功即认领进程唯一 ROOT，首认领后不可变、同根幂等，任何异根解析/认领立即抛结构化错误、绝不更新认领值，探针 = 认领值只读口；② 库模式**零环境突变**——`boot` 先经显式 API 认领（认领即校验）、认领成功才动态 import 目标模块，宿主 `process.env` 全程一字不动（CLI 模式本就走 `spawnSync` env 选项注入子进程，不涉宿主环境，保持不变）；③ C3 扩格：三库件交错排列导入、并发导入、import 抛错、认领 API 抛错各态；C7 扩格：首认领成立/同根幂等/异根立即抛/认领后不可变/探针回报认领值。GRILL D4/C3/C7、plan §1.2/§1.4/§3 S1/S3 同步。

## R2-H4 观测基线缺可复现的录制与规范化协议 —— 采信

- 复核：C2 原文只写「规范化 stdout/stderr 全文」与 `loop/` 差量，录制条件与规范化规则全未冻结；`breaker --reset` 写 `startedAt: new Date().toISOString()`（源码核实）、`contract` 输出含绝对路径与 `worktree` 实时集合——规范化不可避免而规则不冻结即可过度删除真实差异；只快照 `loop/` 证不了 `boot` 无树内他处副作用。成立。
- 修订：C2 录制与比对协议六条随 prd 冻结——① 逐案录制清单（初始树夹具/argv/stdin 字节/env 白名单/次序/重置步骤）冻结为数据文件；② 每案在隔离测试树执行；③ 原始输出与规范化输出并存；④ 规范化 = 字段级白名单（逐字段登记替换规则，白名单外一字不动），规范化器脚本自身入冻结面；⑤ 比对整个测试根的路径与字节差量、声明写集之外零变化；⑥ 语义扰动反向用例证明规范化不吞真实行为差异。GRILL C2、plan §1.6/§2.1/§3 S2 同步。

## R2-M1 D5 对普通 CLI 的故障分类自相矛盾 —— 采信

- 复核：D5 故障域把语法损坏/Node 不兼容列为引导失败（应归 64），表格又要求数值码原码透传，C4 还要求这些故障 CLI 出 64——而子进程语法损坏从外部只见 `status 1`，与业务 RED 不可区分。矛盾确认，成立。
- 修订（取「原码透传」分支，不取带外诊断协议——零依赖内核不为此造协议，且 `kit-lock` 内容校验已把受支持路径上的包文件损坏在 spawn 前拦下、残余暴露面收窄到锁冻错/Node 环境不兼容）：分类改按**侦测点**划界——spawn/import 前可确定性侦测的引导失败逐入口归一（CLI 64）；子进程一经派生，数值退出码对普通 CLI 一律原码透传，代价记档；`status === null` 且无信号无 error 补入引导失败分支（CLI 64 / guard 2 / lint WARN+0）。guard 与 lint 行为不受影响（本就全归一）。GRILL D5/C4、plan §1.4/§3 S3 同步。

## R2-M2 `kit-lock` 完整清单语义未定义 —— 采信

- 复核：C0 原文只点名 `bin/*.mjs` 与 `lib/root.mjs`，包内 `package.json`/`README.md` 是否入锁、多余文件是否拒绝、软链接/路径穿越如何处置均未写。成立。
- 修订：锁语义 = 除 `.git` 外全部常规文件**严格集合相等**（缺件/多件/哈希失配均判失配）、拒软链接等非常规文件、路径规范化并限界包根；C0 全清单化（缺件/多件均红）。检查—执行竞态按 codex 给出的「写明威胁模型」分支处置：锁防漂移与误配（多树/升级不同步/意外脏改），不防校验后毫秒窗口的主动替换，单人本机场景记档接受、不做执行时快照；commit 字段运行时不校验（并入 R2-M3 处置）。GRILL D4/C0、plan §1.5/§3 S1/§6 `kit-lock` 词条同步。

## R2-M3 `kit-lock` 冻结顺序与包仓 commit 字段冲突 —— 采信

- 复核：plan §2 步 1（accept 期生成并冻结锁）先于步 2（包仓落成自提交），锁若含包仓 commit 则冻结时该 commit 尚不存在，事后补写又改已冻结文件。顺序死结确认，成立。
- 修订（取 codex 首选分支「预先可计算的内容摘要 + commit 降为出处」）：锁**纯内容寻址**、自期望存档预计算、不含包仓 commit；commit 降为事后出处信息只记包 `README.md`、运行时不校验。C0 红基线（包不存在）与两阶段门禁次序不需重排。GRILL D4/D9、plan §1.5/§2.1/§6 同步。

## R2-L1 每调用校验的性能断言无量化证据 —— 采信，记债不阻塞

- 复核：route:human #6 原文写「毫秒级」，无实测支撑；codex 实测指示值（DrvFs 冷态：十脚本约 64 KB、空 Node 启动约 0.02 秒、逐文件 sha256 约 0.17 秒）表明成本非毫秒级、且多 hook 叠加放大。成立。
- 处置：`LOW` 按裁定规则记债——plan 新增 §7 挂账节：实现期补冷/热缓存与完整包全清单实测、给出可接受预算，随 route:human #6 呈 Steven 裁定；禁以 `mtime`（文件修改时间戳）缓存换速。route:human #6 文本同步录入实测指示值，删除「毫秒级」无据表述。

## 收口判定

- 8 条发现全数处置：7 条（4 `HIGH` + 3 `MED`）修入 GRILL/plan，1 条（`LOW`）记债挂账。
- 重大设计变更：无——薄 `shim`、`kit-lock`、观测基线、D5 加严方向四支柱原样；Steven 已裁定的四项不受影响。
- 按 Steven 2026-07-13 裁定（route:human #1「有条件签署，条件 = round-2 codex 设计审收口」）与本轮裁定规则（全部 findings 处置完且非重大变更 → 放行）：**round-2 收口，实现开工闸放行**。开工仍按 plan §2 次序：先 `contract` 声明入口分流与红基线，再动实现字节。
