# 智能体工具首纵切集成决策

## 已知输入

- 目标基线固定为 `dev@5cd75d5`。
- 遗产候选固定为 `da7b2f1` 中七个智能体工具原子及其零 `SUT` 验收。
- 不搬旧实现、旧报告、旧录屏、`trace`、目标地址或凭据。

## 决策

1. 逐提交重放候选实现，不整树覆盖；冲突以目标基线已经落地的删除因果绑定、意图失败折叠和正式报告交付为准。
2. 保留七原子的 `fail-closed`（安全失败）参数闸、域锁、身份回读和删除后归零约束。
3. 验收只运行明确标为零 `SUT`、静态、schema 或纯函数的命令；不启动、连接或回放任何假 `SUT`、夹具或真实 `SUT`。
4. 真机编译、人签、回放、录屏、视觉复核与独立单用例 HTML 全部保留为 `route:human`，本集成不得冒充行为完成。

## 停止条件

候选提交无法在不削弱目标基线信任边界的前提下重放，或任何验收命令无法证明为零 `SUT` 时，停止并上报，不改用假环境补证。
