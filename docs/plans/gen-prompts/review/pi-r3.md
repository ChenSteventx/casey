# gen-prompts 修复层评审（r3）——pi

调用式：首次调用 `pi -p --no-session --no-tools --thinking high --model deepseek-v4-pro @docs/plans/gen-prompts/review/material-r3.md <指令>` 因模型在 `--no-tools` 模式下仍尝试发起 `<read_file>` 工具调用而中断（响应不完整、未给出结论）；改用明确告知"你没有工具调用能力，只依据已提供文本作答，不要尝试调用任何工具"的修订指令重跑后正常完成。评审对象：round-2 双路复核结果，同一份 `material-r3.md`。实现方 Claude，评审方 pi/deepseek-v4-pro（异构冗余，Dissimilar Redundancy，评审族≠实现族）。

---

# 异构冗余 round-3 评审结论（codex·pi 之路）

**结论：PASS**
（仅基于 stdin 提供的 `dispositions-r2.md`、`diff`、门禁证据做文本级逻辑核实；无任何工具调用。）

## 一、codex round-2 4 条缺陷逐条兑现核实

### 1. A1（HIGH）真实地址字面量留痕
兑现状态：已兑现，无残留证据可推翻。`dispositions-r1.md` 第 14 行已改为脱敏描述；门禁报告"全仓地址字节串检索 0 处命中"；本次评审 stdin 全文亦未见任何可识别的真实地址字面量。

### 2. A6（MED）地址等价编码漏检——三种新形态
兑现状态：已兑现，正则改动与处置表验证自洽。`scanEquivalentEncodings` 的 diff 变更能匹配 codex 给出的三个具体反例，文本级逻辑一致，无自相矛盾。

### 3. A6-衍生假阳性（版本号/小数误判内网地址）
兑现状态：已兑现，假阳性构造已从候选提取中消除。`DOTTED_QUAD_RE`（恰好四段）使 `10.20`、`10.5` 等两段小数不再被提取为候选。代价是放弃了对"两三段点分简写"的检测（已声明为该类静态扫描的公认残余缺口），属合理的工程取舍，未引入功能性回归。

### 4. A7（MED）N1b 去注释绕过 → N1c 结构化 spawn 调用点计数
兑现状态：已兑现（对 codex 给出的具体绕过构造有效），且置有金丝雀。codex 给出的绕过构造必然会被 `SPAWN_CALL_RE` 匹配，基线突破即判红；N1d 金丝雀自证检查有牙且局限真实。

## 二、round-2 修复本身是否引入新缺陷

正面肯定：逻辑完备，未见自相矛盾或明显破坏。所有正则变更均仅影响私网地址扫描，且构成"候选提取→归一化→权威清单对比"的标准流水线；点分收紧到恰好四段不会遗漏标准四段点分十进制地址。

待关注的次要项（不影响 PASS 结论，但建议纳入后续迭代）：**`SPAWN_CALL_RE` 遗漏 `fork`**——Node `child_process.fork()` 同样可用于启动模块，其调用点不会匹配现有正则。由于 N1c 已明确声明"非无懈可击"，此遗漏不属于 round-2 修复引入的"功能性破坏"，而是防线覆盖面可进一步扩展。

## 三、铁不变量核实

裁判零 LLM / authoring 不进闭包 / 零 API key·网络·凭据接线 / 无真实地址泄露——四项均判通过。

**综上，codex round-2 的 4 条缺陷已逐条兑现，修复未引入明确的阻断性新缺陷，铁不变量完整成立。本轮评审通过。**

---

## 驱动员核对（sonnet5，修复者本人）

pi 独立指出的 `fork()` 遗漏与 codex round-3 第 1 条部分重合（pi 判定为"次要、不影响 PASS"，codex 判定为"足以构成 A7 未关闭"）——两路方向一致、严重度判断不同，属正常的异构评审覆盖差异。已按 codex 更严格的处置方式一并修复（补 `fork` + 新增 N1e 正向允许清单），见 `docs/plans/gen-prompts/review/dispositions-r3.md`。pi 本轮技术分析部分与 round-2 高度重叠（复用了对 round-2 处置表的核实），首次调用因 CLI/模型行为不匹配（`--no-tools` 模式下模型仍尝试工具调用）中断的问题已用更明确的指令规避，记录在案供后续轮次参考。
