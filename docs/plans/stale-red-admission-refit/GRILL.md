# stale-red-admission-refit — GRILL（light）

## 问题是什么

实体语义锁准入面落地后，`report-diagnostics`（11/12）与 `p3-compile`（4/14）两冻结金牌陈旧红。红因均为夹具侧 `casey run` / `compile --execute|--verify` 未携新准入件（冻结实体锁 / 预执行身份权威 / `--skip-login` 受众匹配），非渲染或编译逻辑回归；stash 实证红先于本日报告模板改动。

## 为什么现在修

- 两金牌盖着 P3 编译门与 P7 诊断栏目的被证义务，长期红会掩盖真回归（陈旧红=盲区）。
- 修法先例已冻：`entity-ui-wiring.searchopen`（铸权模板）与 `bindagent-lockchain`（锁链模板）证明夹具可用生产接缝合法铸测试受众准入件。

## 入口分流

- 生产代码零改 → 非 full；两金牌 + 两 prd 修单、含异构咨询与评审 → 非 direct；取 light。

## 关键取舍（送 `gpt-5.6-sol` max 咨询后拍板）

1. E1 修形：甲=改只读白名单信封（更小，但动事件形状）vs 乙=铸 audience=test 冻结锁（保形，但引入锁链样板）。
2. `p3-compile` 各子检查加 `--skip-login`+铸权后，被证义务是否漂移（尤其 C8「多匹配拒产出」——须确保红因仍是多匹配而非准入拒绝）。

## 风险

- 修单误弱化义务：以「逐子检查义务对账表」防（评审输入）。
- 临时 prd 残留：沿用金牌 `wx` 独占 + finally 清理先例。
- 频道纪律：`codex` 评审（家族≠实现家族）。
