# GRILL · wf-open-observation-yield（证据定案）

1. **缝的事实**：十二跑（`b4r120807j`，22s）删除搜索修真机达阵（计数门通过），停在
   C3 破坏性目标连续性守卫：「同名观察多条（create subject + open source 同 flow 各产
   一条身份观察）→ 不取 first、不武装 ref」fail-closed。守卫无缺陷——同名多条时拒绝
   二选一正是其 codex round-3 Critical 收口语义。
2. **为何现在才可达**：open 读回观察是读回门契约（`a507f5c`）首次引入；运行 11 已产
   双观察但停在计数门（更早层），十二跑计数门通了才触达连续性守卫。且 wiring 金牌
   H1i 早已钉死「多身份原子同流 → issuer 单信封无法表征 → fail-closed」——即使守卫
   放行，成品段 issuer 门也会拦。双观察在单流里是结构性 fail-closed，非偶发。
3. **岔口已裁（Steven 2026-08-07 甲案）**：open 观察让位——同 flow 已有同 platformId
   的 created-in-run subject 观察时，open 跳过归档自己的 source 行；点击前双证照跑
   （扫描+物理双锚+判定表裁定+过门才点，验证价值一分不少），只少归档一行。
   连续性 ref 取 create 观察铸造（唯一）、issuer 单原子（create）走 H1b/H1h 已钉通道。
   乙案（C3 守卫改值唯一）动破坏性守卫语义、爆炸半径大，弃。
4. **open 独跑面不回归**：flow 无 create（打开既有工作流）时 `identityObservations`
   无同 platformId 行 → open 照旧归档 source 观察（successor amendment 的 open [source]
   语义保留）。
5. **冻结面零接触**：改动全在 `compileWorkflowOpen` 声明分支（读回门契约新代码）；
   H1i 纯函数、C3 守卫、wiring 金牌、readback 契约金牌（其 S1e 场景为 open 独跑流，
   断言不受让位影响）均不动。
