# 会话强制重启恢复文档（2026-07-13）

> 因 WSL localhost 中继（`wslrelay.exe`）半死、隧道数据面断，Steven 决定 `wsl --shutdown` 根治。
> 关机会杀掉本 session（Claude Code 跑在 WSL 里）+ 后台 B/C 编排 + 隧道监听器。
> 本文档是给**重启后新 session** 的接续入口。新 session 开场：先 `/starter` 对齐，再读本文件按【下一步】续。
> 状态事实以 `docs/HANDOFF.md` + `loop/active-contract.json` + `contract list` + `git status --short` 为准；本文件是执行指令。

## 本 session 干到哪了（Steven 要「A+B+C+D 全做」）

Steven 2026-07-13 拍板：四项一起做，走 ultracode 编排。模型分工（已记忆 [[ultracode-agent-hierarchy]]）：
- 总编排 + 各契约规划 + 两处汇裁 = fable @ xhigh；
- 执行 = sonnet5 走 ultracode（派子执行，自己守耦合核心）；子执行 = sonnet5 @ xhigh，叶子、不得再派；
- 评审多路：plan 设计审 = codex `gpt-5.6-sol` @ max；代码审 = codex `gpt-5.6-sol` @ medium **并行** pi deepseek v4pro @ high；loop 内 test 结果分析 = codex `gpt-5.6-sol` @ medium（咨询、裁判零 LLM 不破）。pi 已可从 WSL 驱（v0.80.3，旧「驱不动」记录作废）、独有可信新发现并入、缺席不阻塞。

### 编排流水线（8 段，Steven 三条增强已折进 docs/plans/_session-resume/bc-contracts-workflow.js）
`Plan(fable xhigh 读现成 plan.md)` → `PlanReview(codex sol max 异族设计审，Steven 加)` → `PlanArb(fable xhigh 汇裁+必要时改 plan)` → `Build(sonnet5 ultracode + 内嵌 codex sol medium test 分析，Steven 加)` → `Review(codex sol medium ∥ pi deepseek-v4pro high 双路)` → `ReviewArb(fable xhigh 汇裁所有 codex+pi 意见，Steven 加)` → `Fix(sonnet5 + round2 双路复核)` → `Learn`。
护栏：① 裁判零 LLM——build 内 codex test 分析只作 L2 测试质量咨询，gate.mjs 仍唯一写 passes、verdict.mjs 字节不动；② 自评张力——fable 两处汇裁默认采信跨族 codex/pi findings、只有具体反证才驳，且修完仍由 codex+pi round-2 跨族复核（fable 非最后一道闸）。脚本已 node async 包裹语法核验通过。

### D 归档清理 —— 已完成（无需重做）
- 移除三棵已合并契约的旧 worktree（casey-wf-set-node-field / casey-replay-nth-visible-hardening / casey-regress-promptset）；
- 删十个已核实合并进 dev 的契约分支（casey-demo/casey-doctor/distribution/ingest-scaffold/mcp-parity/regress-promptset/replay-nth-visible-hardening/resolution/wf-select-node-dropdown/wf-set-node-field）；
- 保留：`run-convention-claude`（未合并存档分支）+ `archive/codex-wip-*` 三 tags。

### B + C 契约 —— grill+plan 已落盘，Build 未完成（关机丢 Build，需重起编排）
两棵 worktree 都在（`/mnt/d` 持久，关机不丢），baton 各 2/6（grill+plan done）：
- `/mnt/d/ctx/heren/casey-drawer-lock-hardening`（分支 drawer-lock-hardening，lane light）
  - 挂账：codex-sol MED#2，openNode/selectNodeDropdown/setNodeField 三原子域锁从宽 `.hr-drawer__content-wrapper` 收窄；
  - 规划已定（GRILL D2）：**拒**挂账建议的 `.lf-node-drawer` 类锚（真机未采样、仅夹具自造=倒裁），取「可见 `.hr-drawer__content-wrapper` 且内含当前节点标题精确文本」标题锚，复用 openNode 双证回读锚下沉为三原子共用域锁；缺 nodeName 一律 fail-closed 不回落（D4）；openNode 加预点基线归因守卫（D5）；fake-sut 加 twinfield/twinboth/twintitle 三反面场景；红先行金牌 G1–G7；重签 4 份 prd（含 server.mjs 双冻 p5-replay + replay-nth-visible-hardening）。
  - 产物：`docs/plans/drawer-lock-hardening/proposed/GRILL.md` + `plan.md`（已落盘）。
- `/mnt/d/ctx/heren/casey-gen-prompts`（分支 gen-prompts，lane full）
  - regress scope C 改形态（Steven 定）：合成走 **CLI 外 LLM**（coding agent 做），casey CLI 只两段确定性零 LLM——`casey promptset-seed`（出合成种子模板：生成指引+候选格式说明，字节稳定、双侧凭据门）+ `casey promptset-freeze`（校验候选闭合白名单 fail-closed + 幂等冻结追加进 promptset.json：已有 id 绝不覆盖、失败不写盘、原子写、强制 source:llm）。
  - 铁不变量：authoring 绝不进回放/裁定闭包；零 API key/零网络/零凭据/零内网地址（regress 原版直调 API 的 --base/--key/deepseek-v3 默认一概不搬）；verdict.mjs 字节不动。
  - 涟漪：lib/promptset.mjs SOURCES 扩 llm、cli-mcp-face 金牌 EXCLUDED 追加两命令（cli-mcp-face.golden.mjs 双冻于 prd-cli-mcp-face + prd-mcp-parity，重签一个不漏）、CONTEXT 登记三新词条。红先行金牌 S1–S4/F1–F4/P1/N1/C1。
  - 产物：`docs/plans/gen-prompts/proposed/GRILL.md` + `plan.md` + `SCOPE-OPTIONS.md`（已落盘）。

**编排脚本备份**：`docs/plans/_session-resume/bc-contracts-workflow.js`（本 session 用的 Workflow 脚本副本，含两契约 brief、五阶段 pipeline、评审双路 codex+pi、schema）。原件在 `~/.claude/projects/-mnt-d-ctx-heren-casey/0824e954-0e48-4a85-b720-4e5176006c01/workflows/scripts/`（同 session 才能 resumeFromRunId；跨 session 失效）。

### A 真机 UAT —— 卡在相位0 关三，待隧道根治后续
- 关一 doctor 就绪级绿 ✓；关二 Steven 已带外确认 .auth = autotest ✓；关三隧道数据面断（wslrelay 半死）✗。
- 本 session improvise 的绕行（VM-IP 绑定 + Hyper-V 防火墙规则 + stdio 桥）**已全部清除**（文件删净、两条防火墙规则 casey-tunnel-supply/casey-tunnel-reverse-supply 已 UAC 删除、残留 Windows node 进程已杀）。环境回干净。
- runbook：`docs/runbooks/real-uat-runbook.md`（四子项：①历史绿转非绿追因 tc_catalog_wf_crud+tc_wf_publish_states / ②示教三环人录 / ③ingest-scaffold 全新用例端到端 / ④各家 agent MCP 挂载）。真机唯一许用账户 = autotest。

## 未提交现场（别动别并入契约提交）
`M .gitignore`（Steven 的）+ `M loop/prd-selftest.json`（无关 gate 时间戳）+ `?? docs/codex/`（并行 codex 会话产物）+ `?? docs/plans/regress-strategy/`、`?? docs/plans/usability-audit/`（草稿）。
本 session 新增未跟踪：`docs/plans/_session-resume/`（本恢复文档 + 编排脚本副本，非契约、可留可删）。

## 下一步（重启后新 session 按序）

1. **拉标准隧道**（wslrelay 已随 wsl 重启自愈，原版脚本直接好用）：
   - WSL 侧后台：`node scripts/wsl-reverse-listen.mjs`（监听 15519/15520）；
   - Windows 侧：Steven 双击 `scripts/win-forward-start.cmd`（或跑 `node.exe D:\ctx\heren\casey\scripts\win-reverse-agent.mjs`）；
   - 验：`node bin/casey.mjs doctor` 隧道项 + WSL 侧 `ss -tn state established | grep -c :15520` 应 >0 + `curl --max-time 15 http://127.0.0.1:15519/` 真通（HTTP 非 000）。三关全绿才驱真机。
2. **重起 B/C 编排**：直接 `Workflow({scriptPath: "docs/plans/_session-resume/bc-contracts-workflow.js"})`。脚本 Plan 段已写成「读现成 plan.md/GRILL.md 核对即过、不重做」，故 grill+plan 不重跑；PlanReview→PlanArb 会对现成 plan 补跑设计评审（Steven 加的增强，之前那版 plan 没经过设计审，正好补上）。评审多路配置全在脚本里。（注意：Workflow resumeFromRunId 同 session 限定、跨重启失效，所以是「重新 launch」不是「resume」，plan 段靠读盘幂等。）
3. **A 真机四子项**（Steven 在场）：相位0 三关全绿后按 runbook 逐子项，凭据零回显、--sut 只喂 127.0.0.1:15519 回环。
4. B/C 各自绿 + 评审干净后**合并收尾**（Task #5）：主树顺序合并 + 3-way + 合并后重签共享冻结 + 全仓 ratchet 总核 + tier1；**文档先于 dev 提交**（HANDOFF/NEXT-SESSION 刷新后再提交）。

## Task 看板（本 session 建，重启后 task 列表会清空，据此重建或凭本文档跟踪）
1. A 相位0+子项① （in_progress，卡关三）
2. A 子项②③④（pending）
3. B drawer-lock-hardening（in_progress，grill+plan done）
4. C gen-prompts（in_progress，grill+plan done）
5. 合并收尾（pending）
