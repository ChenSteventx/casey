# 首纵切候选资产

`agent-tool-add.flow.json` 只迁移自然语言业务顺序和 Casey 原子映射，实体前缀已改为 `atl_`。它不是已签用例、不是冻结断言，也没有真机 PASS。

进入正式链路前仍须：从当前自然语言重新归一 `TestCase`、人工确认 flow、联网真机编译、草拟并人工签署断言、真实回放、录屏、视觉复核和独立单用例 HTML。目标 MCP 或工具缺席时应判 `NEEDS_HUMAN`，不得沿用遗产旧绿。
