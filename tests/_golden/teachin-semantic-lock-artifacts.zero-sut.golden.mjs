#!/usr/bin/env node
// SECURITY_REVOKED_GOLDEN_TOMBSTONE
// Revoked (SUPERSEDED, benign — not a security hazard): the former body imported and asserted against a flat lib/entity-semantic-lock-artifacts.mjs API that never shipped; the record→distill→compile→sign identity-sidecar chain rerouted to the sidecar-successor authority face.
console.error('SECURITY_REVOKED: use teachin-entity-binding-sidecar-successor successor (auxiliary teachin-semantic-lock / teachin-semantic-lock-admission-authority)');
process.exit(78);
