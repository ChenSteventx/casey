#!/usr/bin/env node
// SECURITY_REVOKED_GOLDEN_TOMBSTONE
// Revoked (SUPERSEDED, benign — not a security hazard): the former body imported and asserted against a flat lib/entity-semantic-lock-runtime.mjs API that never shipped; the preflight/live-rebind/mutation runtime authority rerouted to the runtime-authority successors.
console.error('SECURITY_REVOKED: use teachin-semantic-lock-runtime-authority / teachin-runtime-authority-bundle-successor successor');
process.exit(78);
