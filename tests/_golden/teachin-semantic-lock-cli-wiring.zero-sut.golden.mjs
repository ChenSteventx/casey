#!/usr/bin/env node
// SECURITY_REVOKED_GOLDEN_TOMBSTONE
// Revoked (SUPERSEDED, benign — not a security hazard): the former body statically pinned bin/*.mjs to a flat five-stage CLI token set that never shipped; the record→distill→compile→sign→replay CLI wiring rerouted to the CLI-authority live baseline.
console.error('SECURITY_REVOKED: use observation-cli-authority-wiring successor');
process.exit(78);
