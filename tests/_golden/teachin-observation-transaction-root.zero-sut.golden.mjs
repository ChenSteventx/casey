#!/usr/bin/env node
// SECURITY_REVOKED_GOLDEN_TOMBSTONE
// Revoked because the former body could recursively delete a fixed user case without an owned lease.
console.error('SECURITY_REVOKED: use observation-runtime-trust-root / teachin-observation-safe-case-lease-v2 successor');
process.exit(78);
